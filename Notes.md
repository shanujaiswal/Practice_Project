mkdir "project_name" -- to create a new directory.
cd project_name  -- To enter inside the file.
touch fileName.extension -- to create a new File (For Bash)
New-Item fileName.extension -- to create a new File(For PowerShell)
python -m venv venv  -- Create the enviroment folder 
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process  -- It temporarily lowers Windows security blocks so you can run your local virtual environment scripts in your current terminal window.
env\Scripts\activate  -- Activate the Environment
New-Item app.log  -- to create app.log 
--> Function: It is a plain text file that acts as a digital diary for your automation scripts.Storage: It stores a permanent, step-by-step history of events, timestamps, and messages generated by script_worker.py.Purpose: It bridges the two scripts, allowing script_monitor.py to read and display what happened in the worker script without them running at the same time.

Clear-Content app.log -- To Clear content of app.log

pip install playwright --  Run this command to download the automation engine tool
playwright install chromium  -- Run this command to install the automated version of Google Chrome: